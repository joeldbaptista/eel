sqrs(r)
{
    let s=0;
    for (let k=0, len=#r; k < len[0]; ++k) {
        s += r[k];
    }
    return s;
}

main()
{
    let n = 100, r[n];
    //let r = [1, 2, 4, 8, 16, 32];

    for (let k=0; k < n; ++k)
        r[k] = k**2;
    print("sqrs(r) = %s", sqrs(r));
}
